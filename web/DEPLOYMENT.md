# Vercel deployment

Set the Vercel project Root Directory to `web`.

Required environment variables:
- `DATABASE_URL`
- `AUTH_SECRET`

Also configure the Supabase variables used by the application.

After replacing the project files, create a fresh deployment without reusing the previous build cache.
